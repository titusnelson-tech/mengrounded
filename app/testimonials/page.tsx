import { notFound } from "next/navigation";

// Placeholder testimonials are suspended until real quotes exist.
// The old content is preserved in git history (see previous commits).
export default function Testimonials() {
  notFound();
}
